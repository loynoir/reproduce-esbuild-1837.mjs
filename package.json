{
  "name": "reproduce-esbuild-1837",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run build:esm && pnpm run build:iife",
    "build:esm": "esbuild --bundle ./index.mjs --inject:./shims.mjs --format=esm --outfile=./dist/output.mjs",
    "build:iife": "esbuild --bundle ./index.mjs --inject:./shims.mjs --format=iife --outfile=./dist/output.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "loynoir",
  "license": "ISC",
  "dependencies": {
    "@babel/types": "^7.16.0",
    "esbuild": "^0.14.2",
    "process": "^0.11.10"
  }
}
